{
  "name": "puhutko-lite",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun run --filter @puhutko/server dev",
    "dev:cli": "bun run --cwd apps/cli dev",
    "check": "bun run --filter @puhutko/shared check && bun run --filter @puhutko/sqlite check && bun run --filter @puhutko/recent-searches check && bun run --filter @puhutko/word-example check && bun run --filter @puhutko/word-tags check && bun run --filter @puhutko/wiktionary check && bun run --filter @puhutko/kaikki check && bun run --filter @puhutko/server check && bun run --cwd apps/cli check"
  }
}
